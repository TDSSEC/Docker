FROM debian
RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y \
RUN git clone https://github.com/DanMcInerney/xsscrapy /opt/xsscrapy && \
    git clone git clone https://github.com/AzizDammak/DorkScan ~/DorkScan
